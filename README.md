## exploring node js fs module
This is a simple project to explore the Node.js `fs` (file system) module. The `fs` module provides an API for interacting with the file system in a manner closely modeled around standard POSIX functions.
